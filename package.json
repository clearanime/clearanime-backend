{
  "scripts": {
    "clean": "yarn rimraf site",
    "build": "yarn clean && yarn webpack",
    "run-dev": "yarn clean && yarn webpack-dev-server --open --https"
  },
  "devDependencies": {
    "@swc/core": "^1.2.133",
    "@wasm-tool/wasm-pack-plugin": "^1.6.0",
    "copy-webpack-plugin": "^10.2.1",
    "rimraf": "^3.0.2",
    "swc-loader": "^0.1.15",
    "webpack": "^5.66.0",
    "webpack-cli": "^4.9.1",
    "webpack-dev-server": "^4.7.3"
  },
  "dependencies": {
    "hls.js": "^1.1.3",
    "wasm-pack": "^0.10.2"
  }
}
